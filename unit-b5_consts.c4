// Test Case B5: This testcase demonstrates the capabilies of C4Chinese in declaring and using constants.

hao main(){

  // Constants can be declared similarly as variables, but with the keyword "yongyuan" appended at the start.
  yongyuan hao myIntConst = 5;
  geini(myIntConst);

  // Constants cannot be assigned a value after declaration.
  // As such, the following line would throw a semantic error.
  // myIntConst = 10;

  huijia 0;
}